import { foodBuilder } from './food-builder.contract';
import { locale } from './locale.contract';
import { splitList } from './split-list.contract';
import { splitWord } from './split-word.contract';
import { synonymSet } from './synonym-set.contract';

export default {
  locale,
  foodBuilder,
  splitList,
  splitWord,
  synonymSet,
};
