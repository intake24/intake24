# Import / Export

System allows to import and export locale specific data.

Formats:

1. `JSON` - for tree-like data, e.g. food databases with category / food records and their properties
2. `CSV` - for tabular data, e.g. foods, nutrients, recipe foods, split lists, split words, synonym sets
