import { foodBuilder } from './food-builder.router';
import { locale } from './locale.router';
import { splitList } from './split-list.router';
import { splitWord } from './split-word.router';
import { synonymSet } from './synonym-set.router';

export default { locale, foodBuilder, splitList, splitWord, synonymSet };
